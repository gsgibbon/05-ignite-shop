import { ScrollRight } from "@/styles/components/scrollButton";
import { CaretRight } from "@phosphor-icons/react";

export default function ScrollNext() {
  return(
      <ScrollRight>
        <CaretRight size={48} />
      </ScrollRight>
  )
}